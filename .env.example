# ── Local dev (.env) ──
# Data API (quiz-be) — wrangler dev mặc định cổng 8789
VITE_API_URL=http://localhost:8789
# Senlyzer content-lock API — chạy dịch vụ Senlyzer local cổng 8787
VITE_SENLYZER_API=http://localhost:8787

# ── Production (.env.production) ──
# VITE_API_URL=https://quiz-be.<account>.workers.dev   # điền sau khi deploy quiz-be
# VITE_SENLYZER_API=https://admin-campaign-be.hoangdinhlamhai1.workers.dev
